<%= form_for @message do |f| %>

	<%= f.label :body %>
	<%= f.text_area :body %>

	<%= f.select :user_tokens, @users.collect {|x| [x.username, x.id]}, {}, :multiple => true%>
	<%= f.submit "Sent" %>

<% end %>